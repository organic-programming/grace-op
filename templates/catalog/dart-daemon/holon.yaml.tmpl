schema: holon/v0
uuid: "{{ .UUID }}"
given_name: "{{ .GivenName }}"
family_name: "{{ .FamilyName }}"
motto: "{{ .GivenTitle }} {{ .FamilyTitle }} daemon."
composer: op
clade: deterministic/io_bound
status: draft
born: "{{ .Date }}"
parents: []
reproduction: assisted
generated_by: op
lang: dart
description: Dart scaffold generated by `op new --template`.
kind: composite
build:
  runner: flutter
requires:
  commands: [flutter, dart]
  files: [pubspec.yaml]
artifacts:
  primary: build/linux/x64/debug/bundle/{{ .Slug }}
