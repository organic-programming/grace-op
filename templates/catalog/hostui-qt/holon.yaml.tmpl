schema: holon/v0
uuid: "{{ .UUID }}"
given_name: "{{ .GivenName }}"
family_name: "{{ .FamilyName }}"
motto: "{{ .GivenTitle }} {{ .FamilyTitle }} host UI."
composer: op
clade: deterministic/io_bound
status: draft
born: "{{ .Date }}"
parents: []
reproduction: assisted
generated_by: op
lang: cpp
description: Qt host UI scaffold.
kind: composite
build:
  runner: qt-cmake
requires:
  commands: [cmake]
  files: [CMakeLists.txt]
artifacts:
  primary: build/{{ .Slug }}.app
