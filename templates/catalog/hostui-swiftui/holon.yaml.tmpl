schema: holon/v0
uuid: "{{ .UUID }}"
given_name: "{{ .GivenName }}"
family_name: "{{ .FamilyName }}"
motto: "{{ .GivenTitle }} {{ .FamilyTitle }} host UI."
composer: op
clade: deterministic/io_bound
status: draft
born: "{{ .Date }}"
parents: []
reproduction: assisted
generated_by: op
lang: swift
description: SwiftUI host UI scaffold.
kind: composite
build:
  runner: swift-package
requires:
  commands: [swift]
  files: [Package.swift]
artifacts:
  primary: .build/debug/{{ .Slug }}.app
