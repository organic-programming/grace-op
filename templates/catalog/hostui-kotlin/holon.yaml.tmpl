schema: holon/v0
uuid: "{{ .UUID }}"
given_name: "{{ .GivenName }}"
family_name: "{{ .FamilyName }}"
motto: "{{ .GivenTitle }} {{ .FamilyTitle }} host UI."
composer: op
clade: deterministic/io_bound
status: draft
born: "{{ .Date }}"
parents: []
reproduction: assisted
generated_by: op
lang: kotlin
description: Kotlin host UI scaffold.
kind: composite
build:
  runner: gradle
requires:
  commands: [java]
  files: [build.gradle.kts]
artifacts:
  primary: build/compose/binaries/main/app/{{ .Slug }}.app
