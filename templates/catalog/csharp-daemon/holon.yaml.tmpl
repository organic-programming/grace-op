schema: holon/v0
uuid: "{{ .UUID }}"
given_name: "{{ .GivenName }}"
family_name: "{{ .FamilyName }}"
motto: "{{ .GivenTitle }} {{ .FamilyTitle }} daemon."
composer: op
clade: deterministic/io_bound
status: draft
born: "{{ .Date }}"
parents: []
reproduction: assisted
generated_by: op
lang: csharp
description: C# daemon scaffold generated by `op new --template`.
kind: native
build:
  runner: dotnet
requires:
  commands: [dotnet]
  files: [{{ .Slug }}.csproj]
artifacts:
  binary: {{ .Slug }}
