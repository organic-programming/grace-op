schema: holon/v0
uuid: "{{ .UUID }}"
given_name: "{{ .GivenName }}"
family_name: "{{ .FamilyName }}"
motto: "{{ .GivenTitle }} {{ .FamilyTitle }} host UI."
composer: op
clade: deterministic/io_bound
status: draft
born: "{{ .Date }}"
parents: []
reproduction: assisted
generated_by: op
lang: dart
description: Flutter host UI scaffold.
kind: composite
build:
  runner: flutter
requires:
  commands: [flutter, dart]
  files: [pubspec.yaml]
artifacts:
  primary: build/macos/Build/Products/Debug/{{ .Slug }}.app
