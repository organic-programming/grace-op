schema: holon/v0
uuid: "{{ .UUID }}"
given_name: "{{ .GivenName }}"
family_name: "{{ .FamilyName }}"
motto: "{{ .GivenTitle }} {{ .FamilyTitle }} host UI."
composer: op
clade: deterministic/io_bound
status: draft
born: "{{ .Date }}"
parents: []
reproduction: assisted
generated_by: op
lang: csharp
description: .NET host UI scaffold.
kind: composite
build:
  runner: dotnet
requires:
  commands: [dotnet]
  files: [{{ .Slug }}.csproj]
artifacts:
  primary: bin/Debug/net8.0/{{ .PascalSlug }}.app
